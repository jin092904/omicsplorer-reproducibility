# Frozen releases

No frozen release is published yet. A release directory may be committed only after the offline
validator reports `RELEASE GO` and every retained artifact is bound to the manifest by SHA-256.
